{
  "name": "lensline-vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local scripts/local-server.mjs",
    "build": "node scripts/validate-vercel.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run build && npm test"
  }
}
