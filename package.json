{
  "name": "lensline-vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/validate-vercel.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run build && npm test"
  }
}
